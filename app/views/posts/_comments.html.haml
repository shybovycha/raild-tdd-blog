- @post.comments.each do |comment|
    .comment-container
        .header
            = "#{ comment.author } wrote #{ distance_of_time_in_words_to_now(comment.created_at) } ago"

        .body 
            = comment.body